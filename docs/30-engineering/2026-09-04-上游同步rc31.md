---
status: current
record-type: upstream-sync
date: 2026-09-04
---

# 上游同步记录：v1.0.0-rc.23 → v1.0.0-rc.31

## 基线

- 本地基线：`b2ea874dc`（backup 分支 `backup/pre-rc31-sync-20260904`）
- 上游目标：`v1.0.0-rc.31`（QuantumNous/new-api，2026-09-03 发布；main tip 3 个 fix 未纳入）
- 共同祖先：`ab33749ee`（v1.0.0-rc.23）
- 上游规模：868 提交，731 文件，+87,485 / −15,684
- 本地重叠文件：56 个后端 + 42 个 web；上游删除且本地改过：6 个（kling/jimeng/doubao 平台适配器、video_proxy_gemini）

## 合并策略

按《上游代码合并指南》：业务文件整体采用上游；本地 Link/Seedance/合同/品牌接线以单行调用或窄分支重放；主页（features/home）、docs/、TokenAI 品牌为线下保留白名单。

## 本地保留白名单

- 主页：`web/src/features/home/**`（上游未触碰，零冲突直接保留）
- 文档：`docs/**` 本地体系保留；新增吸收上游 `docs/plugin-api/**`（新插件系统参考）
- TokenAI 品牌：`common/constants.go` SystemName、`web/index.html` 标题、`web/src/lib/constants.ts` DEFAULT_SYSTEM_NAME、`controller/topup_waffo.go` 兜底、`web/scripts/validate-branding-dist.mjs`、rsbuild favicon
- 根目录部署杂项：Taskfile.yml、sync-upstream.sh、start.sh、ci.yml 三数据库服务块等

## 重大上游变化与本地下适配

1. **Task 平台 JS 插件化**：原生 kling/jimeng/doubao/ali/gemini/sora/suno/vertex/vidu 适配器删除，改为 `pkg/jsplugin` + `plugins/tasks/*/plugin.js` + `relay/channel/task/jsplugin`。
   - 本地北向 kling/jimeng 合同中间件迁至 `middleware/kling_video_contract.go`、`middleware/jimeng_video_contract.go`
   - payload 类型迁至 `relay/channel/task/{kling,jimeng}/payload_types.go`；废弃的 jimeng 状态机测试删除
   - `channel.TaskAdaptor` 接口变化（FetchTask 收 `*model.Task`、DoResponse→ParseResponse、ParseTaskResult 新签名）已迁移 seedance 适配器，任务快照（VideoUpstream* / SouthboundAdapterVersion / Key）替代旧 body 传参
2. **渠道类型号冲突**：上游 61 分配给 `ChannelTypeTaskPlugin`；本地 Seedance 61→62、异步图片 62→63、Dummy=64（显式；Go const 裸标识符重复字面量而非自增）、退役 Moxing 65。新增一次性迁移 `model/seedance_channel_type_renumber_migration.go`（marker 门控、降序更新 63→65、62→63、61→62；只有 channels 表存类型值；TaskPlugin 行在本 fork 数据库不存在，见函数注释）。前端常量同步 62/63。web 常量硬编码与 adapter version 断言（61:→62:）同步更新。
3. **LogOther 由 map 改为结构化投影**（public/admin/root/audit 分桶）：本地计费日志辅助（customer_contract_billing、billing_statement_snapshot、task_billing_evidence、task_billing_state）迁移到 SetPublic/SetAdmin API；合同计费字段进入 public 桶，evidence/审计进 admin 桶。
4. **计费表达式编译环境宽松化风险**：上游 map 环境使未知标识符静默为 0。新增 `pkg/billingexpr/var_check.go`（UnknownIdentifier），在本地 `settleTaskTieredSnapshot` 结算前 fail-closed，保持「坏冻结表达式→failed+对账」合同（回归测试 TestFrozenExpressionFailureKeepsPrechargeForReconcile 验证）。
5. **Token 指定渠道**：旧 ContextKeyTokenSpecificChannelId 删除，改用 `GetChannelConstraints().AddPin(PinSourceToken)`；ResolveSeedanceChannel 与 customer contract 测试同步。
6. **/v1/videos 路由面**：上游退役经典 create/read 路由（并入插件端点），新增 `GET /v1/videos/:task_id/content`；`router/video_router_contract_test.go` 允许表按上游面+Link 合同更新。
7. **`legacyTaskAction` 常量**：textGenerate 等 → TextToVideo/ImageToVideo 等规范名。
8. 上游删除 `common.ReaderOnly`/`migrateDBFast`/compact 后缀等本地曾引用的符号：readerOnly 下沉为 middleware 包内 3 行助手；其余随上游删除。

## 数据库结论

- 结论：需要更新（已完成）
- 检查范围：rc.23→rc.31 全部 GORM model 变更（TaskPlugin、PerfMetric、SubscriptionPlan SQLite 分支、ClickHouse 日志库、users 64-bit quota 列检查 ensureUserQuotaColumns、LogOther 结构化、TaskPrivateData 字段）
- 迁移与回填：本地新增 `migrateSeedanceChannelTypeRenumber`（channels.type 一次性重编号，marker `migration.seedance_asyncimage_type_renumber_v1`，先于 image relay 迁移执行）；上游 AutoMigrate 增量与本地新表（TaskCreateIdempotency/TaskCreateAttempt/ProviderCostExposure/ChannelAsset*/CustomerModelContract/CustomerContractAudit）合并进 migrateDB
- 运维注意（fail-closed，非迁移）：rc.31 启动时 `ensureUserQuotaColumns` 拒绝 32 位钱包列的旧库，需 DBA 手工升级或临时 `SKIP_64BIT_QUOTA_SCHEMA_CHECK=true`
- 三数据库验证：迁移用 GORM Update/AutoMigrate 表达，SQLite/MySQL/PostgreSQL 均无方言 SQL；CI（ci.yml 已含 MySQL 8.4 + PostgreSQL 16 服务）+ 本地 SQLite 测试通过
- 回滚方案：重编号迁移为 marker 门控的一次性 UPDATE，可逆（反向 65→63、63→62、62→61 同事务）；升级前照常做数据库备份

## 验证结果

- 业务文件一致性检查：通过——`git diff v1.0.0-rc.31 -- <业务目录>` 残余仅本地扩展新文件与最小接线（87 个上游文件的 wiring 差异均为单行调用/窄分支/本地字段追加）
- 上游新增、删除、重命名检查：通过（含 .github/pr-check.yml 删除、electron、README、THIRD-PARTY-LICENSES）
- 后端：`go build ./...` 通过；`go vet ./...` 通过；`go test ./...` 全绿
- relaykit：`GOWORK=off go build/vet/test ./...` 全绿
- 前端：`bun run typecheck` 通过；`bun run build` 通过（含 docs:validate:dist 与 branding:validate:dist=TokenAI 校验）；`bun run test` 全绿（92 文件 / 554 用例，含 5 个本地测试文件 node:test→vitest 转换）
- 其他专项：seedance 适配器接口迁移测试、合同计费对账回归、渠道类型重编号迁移测试、`task docs:check` 与 `task ai:check` 通过

### lint 注记

rc.31 上游基线自身 `oxlint` 存在约 356 个 error（位于上游自有文件）；本次未顺手清理（最小入侵约束），本地触及文件的 lint 水平与同步前一致。lint 不作为本次门禁，后续单独任务处理。

## 最小入侵收敛（2026-09-04 同日追加）

自查后按《硬约束》§7 追加收敛，把本轮识别的越界点修复：

1. 新增 `service/task_video_polling_link.go`：Link 视频轮询的全部决策点
   （冻结版本解析、合同违约→对账、协议脱敏、对账恢复清理、
   `markTaskReconciliationRequired`）下沉到本地新文件；
   `task_polling.go` 的 `updateVideoSingleTask` 只剩 5 处单行/窄分支调用。
   `taskVideoUpstreamQueryBaseURL` 增加 type-default 兜底参数，
   保持上游「快照 > 渠道 > 类型默认」回退链对原生平台不变。
2. `middleware/distributor.go` 合同门控 14 行内联块压为
   `applyCustomerContractDistributeGate` 单行调用（实现在本地
   `customer_contract.go`）。
3. `setting/billing_setting/tiered_billing.go` 恢复为与上游**逐字节一致**
   （0 diff）：`smokeTestExpr` 上游 1 参签名原样保留；Link 合同变体
   `smokeTestLinkExpr` 落在本地 `task_billing.go`（§7 允许的窄重复）。
4. 删除死代码：`frozenVideoBillingFetchContext`（整文件）、
   `taskVideoUpstreamQueryPath`（职责已移入 seedance 适配器任务快照）。

收敛后残留差异：`task_polling.go` 145+/56− → 117+/52−（剩余为上游 CAS/
退款流的语义集成点，不可再外移）；`distributor.go` 33+/2− → 22+/2−；
`tiered_billing.go` 26/12 → **0**。全量门禁复测通过
（go build/vet、go test 55 包全绿）。

## 未解决风险

- 上游 main tip 在 rc.31 之后还有 3 个 fix（ETag、task plugin 503 提示、release 版本解析），下次同步带上。
- rc.31 上游自身 oxlint 基线不通过（约 356 个 error 均在上游文件），lint 治理另立任务。
- 下一收敛候选：`controller/channel.go`（155+/27−）与 `controller/relay.go`
  （128+/25−）的本地扩展密度仍高，需独立任务拆解。
