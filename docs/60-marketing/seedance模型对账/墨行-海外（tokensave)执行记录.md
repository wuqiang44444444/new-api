# 墨行国外（TokenSave）渠道执行记录

## 1. `doubao-seedance-2-0-260128`

### 1.1. 环境与请求

- 验证环境：本地运行实例，SQLite 主数据库 `one-api.db`
- Provider 线路：墨行海外（TokenSave）；渠道 ID `73`
- 渠道名称：`墨行海外 Seedance 2.0-9 折`
- 北向合同：ModelArk V3（`newapi_modelark_video_v3`）
- 南向协议：`tokensave_media_task_v1`
- 客户模型：`doubao-seedance-2-0-260128-tokensave`
- Provider 模型：`doubao-seedance-2-0-260128`
- 平台 Task ID：`task_80lnMbRTUZsJs7doE4P8Dbh9UXVjc7BA`
- 平台创建时间：`2026-08-13 22:54:15`
- 上游调用详情 ID：`202608132254204925544361193814`
- 上游请求时间：`2026-08-13 22:54:20`

本次任务的冻结计费快照和客户请求快照确认：输入模式为纯文本，没有图片、音频或视频参考输入；
生成时长为 4 秒、分辨率为 480p，并启用生成音频。以下是用于本次对账的规范化请求事实，
不是对原始 HTTP 请求体的逐字保存：

```json
{
  "model": "doubao-seedance-2-0-260128-tokensave",
  "content": [
    {
      "type": "text",
      "text": "小猫在抓鱼"
    }
  ],
  "duration": 4,
  "resolution": "480p",
  "generate_audio": true
}
```

### 1.2. 运行结果

| 核验项 | 本次任务事实 |
| --- | --- |
| 运行状态 | `SUCCESS / 100%` |
| 开始时间 | `2026-08-13 22:54:15` |
| 完成时间 | `2026-08-13 23:00:16` |
| 平台观测总耗时 | `361` 秒 |
| 上游调用耗时 | `336.26` 秒 |
| 计费状态 | `settled` |
| 运行结果 | 视频生成成功，结果引用已写入 Task。 |

原始返回结果如下。为避免保存完整媒体 URL，仅对 `video_url` 的值进行脱敏，其余字段和值保持
Task 终态返回事实：

```json
{
  "id": "4f713ad95d774f0485ed2e0a6c37da32",
  "model": "doubao-seedance-2-0-260128",
  "status": "succeeded",
  "content": {
    "video_url": "[已脱敏：真实视频 URL 已写入 Task]"
  }
}
```

### 1.3. 客户请求分类与账单

“我方单价”取平台为该模型设置的价格；“我方总价”按我方单价乘以 4 秒计算；“上游单价”取
TokenSave 实际应用 9 折后的单价；“上游总价”取 TokenSave 本次实际结算金额。人民币统一按
`1 USD = 6.82 RMB` 换算。

| 生成模式 | 我方单价 | 我方总价 | 上游单价 | 上游总价 | 官方单价 | 相对官方比例 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 文生视频；480p；4 秒；生成音频 | `$0.0679/秒`<br>`¥0.463078/秒` | `$0.2716`<br>`¥1.852312`<br>`135800 quota` | `$0.06111/秒`<br>`¥0.416770/秒` | `$0.24444`<br>`¥1.667081` | `$0.07/秒`<br>`¥0.4774/秒` | `87.30%` |

相对官方比例按折后上游单价计算：`$0.06111 ÷ $0.07 = 87.30%`。

### 1.4. TokenSave 上游账单

| 折前金额 | 折扣倍率 | 实际扣费 | 计费状态 |
| ---: | ---: | ---: | --- |
| `$0.2716`<br>`¥1.852312` | `0.90` | `$0.24444`<br>`¥1.667081` | 已结算 |

来源：TokenSave 调用详情 `202608132254204925544361193814`。

### 1.5. 阶段性结论

1. 渠道 73 的 480p、4 秒文生视频请求成功完成。
2. 我方按 `$0.0679/秒` 计费，总价 `$0.2716 / 135800 quota`，计费正确。
3. TokenSave 9 折后实际成本为 `$0.06111/秒`、总价 `$0.24444`，相当于官方单价的 `87.30%`。
4. **账单可以对上：**TokenSave 折前金额 `$0.2716` 与我方总价一致，且
   `$0.2716 × 0.90 = $0.24444`，与上游实际扣费一致，没有差额。
