# 墨行国内（Moxing）执行记录

## 1. `doubao-seedance-2-0-260128-moxing`

### 1.1. 环境与请求

- Provider 线路：墨行国内；渠道 ID `65`
- 北向合同：ModelArk V3（`newapi_modelark_video_v3`）
- 南向协议：`moxing_media_task_v1`
- 客户模型：`doubao-seedance-2-0-260128-moxing`
- Provider 模型：`doubao-seedance-2-0-260128`
- 平台 Task ID：`task_RGzK6KSlXSlZft8leIl44cmh1QWJojQ2`
- 平台创建时间：`2026-08-15 00:09:41`
- 平台创建 Request ID：`202608141609406883490008268d9d6NoPKri0P`
- 墨行调用 ID：`202608150009431349270562003209`
- 墨行密钥名称：`TokenAI`

本次请求为纯文本、4 秒、480p、16:9，同步生成音频：

```json
{
  "model": "doubao-seedance-2-0-260128-moxing",
  "content": [
    {
      "type": "text",
      "text": "雪山真美"
    }
  ],
  "duration": 4,
  "generate_audio": true,
  "ratio": "16:9",
  "resolution": "480p"
}
```

### 1.2. 运行结果

| 核验项 | 本次任务事实 |
| --- | --- |
| 运行状态 | `SUCCESS / 100%` |
| Worker 开始时间 | `2026-08-15 00:09:43` |
| 完成时间 | `2026-08-15 00:13:22` |
| Worker 耗时 | `219` 秒 |
| 平台观测总耗时 | `221` 秒 |
| 墨行账单调用耗时 | `213.24` 秒 |
| 计费状态 | `settled` |
| 运行结果 | Provider 生成成功，结果引用已写入 Task。 |

平台客户合同投影如下。为避免保存完整媒体 URL，仅保留脱敏占位：

```json
{
  "id": "task_RGzK6KSlXSlZft8leIl44cmh1QWJojQ2",
  "status": "succeeded",
  "content": {
    "video_url": "[已脱敏：真实视频 URL 已写入 Task]"
  }
}
```

后续复核确认，先前内容代理返回的 `403` 已解决，视频结果可正常交付，本次生成与内容代理
交付链路均已通过验收。本文不保存 Provider 结果域名或完整签名 URL。

### 1.3. 请求分类与账单

“我方单价”取[墨行国内价格表](../../70-research/墨行/国内/价格.md)中 480p、无视频输入档位；
“上游单价”根据墨行账单实际金额反推。“火山官方价”取火山方舟 `doubao-seedance-2.0`
无视频输入的按量原价 `¥46/百万 Tokens`，并按 480p、16:9、24fps 的单秒 Token 用量
`10,044` 折算为 `¥0.462024/秒`。官方价格来源与统一折算口径见
[Seedance 视频生成与账单验证总览](./Seedance视频生成与账单验证总览.md)；USD 均按
`1 USD = 6.82 RMB` 换算。

| 生成模式 | 我方单价 | 我方总价 | 上游实际单价 | 上游实际总价 | 火山官方原价 | 我方相对官方 | 上游相对官方 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 文生视频<br>480p<br>4 秒<br>无视频输入 | USD：`$0.067741935484/秒`<br>RMB：`¥0.462/秒` | USD：`$0.270967741936`<br>RMB：`¥1.8480`<br>Quota：`135484` | USD：`$0.060967741935/秒`<br>RMB：`¥0.4158/秒` | USD：`$0.243870967742`<br>RMB：`¥1.6632` | Token：`¥46/百万 Tokens`<br>折算 USD：`$0.067745/秒`<br>折算 RMB：`¥0.462024/秒` | `99.9948%` | `89.9953%` |

我方计费核对：

```text
$0.067741935484/秒 × 4 秒 = $0.270967741936
$0.270967741936 × 500000 quota/USD = 135483.870968 quota
四舍五入后 = 135484 quota
```

上游实际折扣核对：

```text
¥1.6632 ÷ 4 秒 = ¥0.4158/秒
¥1.6632 ÷ ¥1.8480 = 90.0000%
¥1.8480 - ¥1.6632 = ¥0.1848
```

火山官方原价对比：

```text
¥46/百万 Tokens × 10,044 Tokens/秒 = ¥0.462024/秒
¥0.462024/秒 × 4 秒 = ¥1.848096
我方：¥0.462 ÷ ¥0.462024 = 99.9948%
上游：¥0.4158 ÷ ¥0.462024 = 89.9953%
```

我方墨行基准价与火山官方折算价仅相差 `¥0.000024/秒`，属于价格表保留位数造成的差异；
上游实际扣费约为火山官方无折扣原价的 `89.9953%`，约等于 `9 折`。

### 1.4. 墨行上游账单

| 模型产品 | 计算参数 | 调用耗时 | 实际扣费 |
| --- | ---: | ---: | ---: |
| `doubao-seedance-2.0` | `4 秒` | `213.24 s` | `¥1.6632` |

墨行页面的 `213.24 s` 是整次调用耗时，不是计费输出时长。实际金额满足
`¥1.6632 ÷ 4 = ¥0.4158/秒`，因此账单仍按请求的 4 秒输出时长核对。账单页未显式给出折扣
倍率，`90.0000%` 为根据实际扣费与基准价反推的结果。

### 1.5. 阶段性结论

1. 渠道 65 的 Seedance 2.0、480p、4 秒文生视频请求已成功完成，`generate_audio=true`未导致失败。
2. 我方按墨行基准价 `$0.067741935484/秒` 计费，总价 `$0.270967741936 / 135484 quota`，
   与计费表达式一致。
3. 墨行上游实际成本为 `¥1.6632`，反推为 `¥0.4158/秒`，相当于我方基准价的
   `90.0000%`；相当于火山官方折算原价 `¥0.462024/秒` 的 `89.9953%`，约为 `9 折`。
4. **账单可以对上：**我方金额满足 `$0.067741935484 × 4 = $0.270967741936`；上游金额满足
   `¥1.6632 ÷ 4 = ¥0.4158/秒`；火山官方同规格 4 秒折算原价为 `¥1.848096`，三方计价口径
   均按 4 秒输出时长计算。

## 2. `doubao-seedance-2-0-fast-260128-moxing`

### 2.1. 环境与请求

- Provider 线路：墨行国内；渠道 ID `66`
- 北向合同：ModelArk V3（`newapi_modelark_video_v3`）
- 南向协议：`moxing_modelark_media_v1`
- 客户模型：`doubao-seedance-2-0-fast-260128-moxing`
- Provider 模型：`doubao-seedance-2-0-fast-260128`
- 平台 Task ID：`task_QBkJMGVETW9ubGmALWY4zWTU2reLKoXD`
- 平台创建时间：`2026-08-15 00:59:35`
- 平台创建 Request ID：`202608141659345361690008268d9d69morMEH0`
- 墨行调用 ID：`2026081500593690096927952554236`
- 墨行密钥名称：`TokenAI`

本次请求为纯文本、4 秒、480p、16:9；客户端未显式传入 `generate_audio`、`seed` 或
`camera_fixed`：

```json
{
  "model": "doubao-seedance-2-0-fast-260128-moxing",
  "content": [
    {
      "type": "text",
      "text": "最后的晚餐"
    }
  ],
  "duration": 4,
  "ratio": "16:9",
  "resolution": "480p"
}
```

墨行 Fast 合同对省略的 `generate_audio` 使用 `true` 作为默认值；`seed` 和 `camera_fixed`
保持缺失，没有进入南向请求。

### 2.2. 运行结果

| 核验项 | 本次任务事实 |
| --- | --- |
| 运行状态 | `SUCCESS / 100%` |
| Worker 开始时间 | `2026-08-15 00:59:37` |
| 完成时间 | `2026-08-15 01:01:26` |
| Worker 耗时 | `109` 秒 |
| 平台观测总耗时 | `111` 秒 |
| 墨行账单调用耗时 | `104.79` 秒 |
| 计费状态 | `settled` |
| 运行结果 | Provider 生成成功，结果引用已写入 Task。 |

平台客户合同投影如下。为避免保存完整媒体 URL，仅保留脱敏占位：

```json
{
  "id": "task_QBkJMGVETW9ubGmALWY4zWTU2reLKoXD",
  "status": "succeeded",
  "content": {
    "video_url": "[已脱敏：真实视频 URL 已写入 Task]"
  }
}
```

墨行终态查询返回的原始 JSON 如下。仅对 Provider Task ID 和媒体 URL 做脱敏，字段结构、时间戳和
`usage` 数值保持原值：

```json
{
  "object": "media.task",
  "task_id": "[已脱敏：Provider Task ID]",
  "status": "succeeded",
  "status_code": 200,
  "capability": "video_generation",
  "model": "doubao-seedance-2-0-fast-260128",
  "created_at": 1786726776,
  "updated_at": 1786726882,
  "result": {
    "type": "video",
    "primary_url": "[已脱敏：媒体 URL]",
    "urls": [
      "[已脱敏：媒体 URL]"
    ]
  },
  "usage": {
    "completion_tokens": 40594,
    "completion_tokens_details": {
      "reasoning_tokens": 0
    },
    "prompt_tokens": 0,
    "prompt_tokens_details": {
      "cached_tokens": 0
    },
    "total_tokens": 40594
  }
}
```

该原始终态响应证明 `40,594` Tokens 不是只能从墨行账单页面取得：查询接口本身已经返回
`usage.completion_tokens=40594`、`usage.prompt_tokens=0` 和 `usage.total_tokens=40594`。
`2026-08-15` 再次使用本任务的冻结查询地址和 Provider Task ID 直查上游终态，返回值仍然一致：

| Token 核验来源 | 输入 Token | 输出 Token | 总 Token |
| --- | ---: | ---: | ---: |
| 首次终态响应 | `0` | `40,594` | `40,594` |
| 再次直查上游终态 | `0` | `40,594` | `40,594` |
| 墨行账单记录 | `0` | `40,594` | `40,594` |

三份证据完全一致，未发现轮询阶段返回临时估算值、终态后 Token 变化或账单使用另一套 Token 数量。

### 2.3. 请求分类与账单

我方、墨行上游和火山官方使用同一无视频输入单价：`¥37/百万 Tokens`，按固定汇率
`1 USD = 6.82 RMB` 换算为 `$5.425219941349/百万 Tokens`。本次差异不在单价，而在结算
Token 数量：墨行首次终态查询和再次直查均返回实际 `usage`，但本次调用发生时的 adapter 以
`IncludeVerifiedUsage=false` 阻止该字段进入平台 Task 结算输入，因此平台保留预估
`324,000 Tokens`；墨行查询响应和账单记录的实际用量均为 `40,594 Tokens`。后续代码已删除该开关，
本节金额仍如实保留本次历史调用的实际结算结果。再次查询只核验 Provider 的历史终态事实，不会追溯
修改该历史 Task 已经完成的平台结算。

| 生成模式 | 我方单价 | 平台结算 Token | 我方总价 | 上游账单/终态复查 Token | 上游实际总价 | 火山官方原价 | 上游相对官方 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 文生视频<br>480p<br>4 秒<br>无视频输入 | USD：`$5.425219941349/百万 Tokens`<br>RMB：`¥37/百万 Tokens` | `324,000` | Quota：`878,886`<br>USD：`$1.757772`<br>折算 RMB：`¥11.988005` | 账单：输入 `0`、输出 `40,594`、总计 `40,594`<br>终态复查：输入 `0`、输出 `40,594`、总计 `40,594` | USD：`$0.220231378299`<br>RMB：`¥1.501978` | USD：`$5.425219941349/百万 Tokens`<br>RMB：`¥37/百万 Tokens` | `100.0000%` |

平台结算核对：

```text
324,000 Tokens × $5.425219941349/百万 Tokens = $1.757771260997
$1.757771260997 × 500000 quota/USD = 878885.630499 quota
四舍五入后 = 878,886 quota
```

墨行实际账单核对：

```text
首次终态 = 再次直查终态 = 墨行账单 = 40,594 输出 Tokens
40,594 Tokens × ¥37/百万 Tokens = ¥1.501978
¥1.501978 ÷ 6.82 = $0.220231378299
$0.220231378299 × 500000 quota/USD = 110115.689150 quota
按平台规则四舍五入后应为 110,116 quota
```

差异核对：

```text
324,000 - 40,594 = 283,406 Tokens
878,886 - 110,116 = 768,770 quota
¥11.988000 - ¥1.501978 = ¥10.486022
324,000 ÷ 40,594 = 798.1475%
```

因此，本次单价和墨行账单均能与火山官方原价对上，但平台没有使用终态查询已经返回的
`40,594 Tokens` 结算，客户侧最终扣费约为按实际 Token 计算结果的 `7.9815` 倍。

### 2.4. 墨行上游账单

| 模型产品 | Token 用量 | 计算参数 | 调用耗时 | 实际扣费 |
| --- | ---: | ---: | ---: | ---: |
| `doubao-seedance-2.0-fast` | 输入：`0`<br>输出：`40,594`<br>总计：`40,594` | `4 秒` | `104.79 s` | `¥1.501978` |

上游金额精确满足 `40,594 × ¥37/百万 Tokens = ¥1.501978`，说明本次墨行 Fast 按输出
Token 结算。再次直查终态仍为输入 `0`、输出 `40,594`、总计 `40,594 Tokens`，与首次终态和账单
记录完全一致；按本次历史账单金额反推，实际单价就是火山国内无视频输入官方原价，没有额外折扣。

### 2.5. 阶段性结论

1. 渠道 66 的 Seedance 2.0 Fast、480p、4 秒文生视频请求已成功完成；任务结果引用已写入平台。
2. 墨行首次终态、再次直查终态和账单记录均为输入 `0`、输出 `40,594`、总计 `40,594 Tokens`；
   Token 数量已完成三方复核，没有差异。
3. 平台单价配置正确，但结算数量仍使用预估 `324,000 Tokens`，最终扣除 `878,886 quota`；按上游
   实际用量应为 `110,116 quota`，本次多扣 `768,770 quota`。
4. **Provider 账单可以对上，但本次客户账单没有对上：**问题不是价格倍率或 Provider 缺少 usage，
   而是调用发生时平台未把墨行终态查询中的 `40,594 Tokens` 投影为结算用量；再次直查不会追溯修改
   历史结算，该代码问题后续已修正。
5. 本次请求没有传入 `seed` 或 `camera_fixed`，与当前墨行 Fast 参数合同一致。

## 3. `doubao-seedance-2-0-mini-260615-moxing`

### 3.1. 环境与请求

- Provider 线路：墨行国内；渠道 ID `67`
- 北向合同：ModelArk V3（`newapi_modelark_video_v3`）
- 南向协议：`moxing_modelark_media_v1`
- 客户模型：`doubao-seedance-2-0-mini-260615-moxing`
- Provider 模型：`doubao-seedance-2-0-mini-260615`
- 平台 Task ID：`task_kxWOaOLwMMat9TtN71WYPdCmDb9JMC5k`
- 平台创建时间：`2026-08-15 02:06:19`
- 平台创建 Request ID：`202608141806194291760008268d9d6wlHMFU04`
- 墨行调用 ID：`2026081502062175325578001132814`
- 墨行请求时间：`2026-08-15 02:06:22`
- 墨行密钥名称：`TokenAI`

本次请求为纯文本、4 秒、480p、16:9；客户端未显式传入 `generate_audio`、`seed` 或
`camera_fixed`：

```json
{
  "model": "doubao-seedance-2-0-mini-260615-moxing",
  "content": [
    {
      "type": "text",
      "text": "我的世界"
    }
  ],
  "duration": 4,
  "ratio": "16:9",
  "resolution": "480p"
}
```

### 3.2. 运行结果

| 核验项 | 本次任务事实 |
| --- | --- |
| 运行状态 | `SUCCESS / 100%` |
| Worker 开始时间 | `2026-08-15 02:06:21` |
| 完成时间 | `2026-08-15 02:08:42` |
| Worker 耗时 | `141` 秒 |
| 平台观测总耗时 | `143` 秒 |
| 墨行账单调用耗时 | `134.93` 秒 |
| 计费状态 | `settled` |
| 运行结果 | Provider 生成成功，结果引用已写入 Task，内容获取未出现 `403`。 |

平台客户合同投影如下。为避免保存完整媒体 URL，仅保留脱敏占位：

```json
{
  "id": "task_kxWOaOLwMMat9TtN71WYPdCmDb9JMC5k",
  "status": "succeeded",
  "content": {
    "video_url": "[已脱敏：真实视频 URL 已写入 Task]"
  }
}
```

本次从墨行原始终态响应中采集到的 `usage` 字段如下。Provider Task ID、媒体 URL 和其它私有响应
字段不在本文保存；字段路径和数值由平台计费用量证据逐项核对：

```json
{
  "usage": {
    "completion_tokens": 40594,
    "completion_tokens_details": {
      "reasoning_tokens": 0
    },
    "prompt_tokens": 0,
    "prompt_tokens_details": {
      "cached_tokens": 0
    },
    "total_tokens": 40594
  }
}
```

原始终态中的 `usage.completion_tokens=40594` 已被平台识别为实际输出用量，私有计费状态记录
`actual_usage_reported=true`、来源为 `usage.completion_tokens`，并据此完成差额结算。

`2026-08-15` 再次使用本任务的冻结查询地址和 Provider Task ID 直查上游终态，Token 返回值保持不变：

| Token 核验来源 | 输入 Token | 输出 Token | 总 Token |
| --- | ---: | ---: | ---: |
| 首次终态响应 | `0` | `40,594` | `40,594` |
| 再次直查上游终态 | `0` | `40,594` | `40,594` |
| 墨行账单记录 | `0` | `40,594` | `40,594` |

首次终态、再次直查和账单记录完全一致，证明本次平台结算采用的 `40,594 completion tokens` 是稳定的
Provider 终态实际用量，不是轮询中的临时估算值。

本次调用由 `2026-08-15 01:58:36` 启动的旧服务进程处理，而清理公开任务数据中内部
`usage_source`、`usage_evidence` 字段的代码在 `02:00:16` 才更新，因此本次记录不能作为这两个内部
字段已从公开响应移除的验收证据；计费用量采集和结算结果不受影响。需在服务重启后的新调用中单独验收
公开响应隔离。

### 3.3. 请求分类与账单

墨行 Mini、我方配置和火山官方无视频输入单价均为 `¥23/百万 Tokens`，按固定汇率
`1 USD = 6.82 RMB` 换算为 `$3.372434017595/百万 Tokens`。本次平台先按预估
`324,000 Tokens` 预扣，终态取得实际 `40,594 completion tokens` 后，已按实际用量结算并退回差额。

| 生成模式 | 我方单价 | 平台实际结算 Token | 我方最终总价 | 上游账单/终态复查 Token | 上游实际总价 | 火山官方原价 | 上游相对官方 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 文生视频<br>480p<br>4 秒<br>无视频输入 | USD：`$3.372434017595/百万 Tokens`<br>RMB：`¥23/百万 Tokens` | `40,594` | Quota：`68,450`<br>USD：`$0.136900586510`<br>RMB：`¥0.933662` | 账单：输入 `0`、输出 `40,594`、总计 `40,594`<br>终态复查：输入 `0`、输出 `40,594`、总计 `40,594` | USD：`$0.136900586510`<br>RMB：`¥0.933662` | USD：`$3.372434017595/百万 Tokens`<br>RMB：`¥23/百万 Tokens` | `100.0000%` |

平台预扣核对：

```text
324,000 Tokens × $3.372434017595/百万 Tokens = $1.092668621701
$1.092668621701 × 500000 quota/USD = 546334.310850 quota
四舍五入后 = 546,334 quota
```

平台实际结算核对：

```text
40,594 Tokens × $3.372434017595/百万 Tokens = $0.136900586510
$0.136900586510 × 500000 quota/USD = 68450.293255 quota
四舍五入后 = 68,450 quota
546,334 - 68,450 = 477,884 quota（退回差额）
```

墨行账单与火山官方原价核对：

```text
首次终态 = 再次直查终态 = 墨行账单 = 40,594 输出 Tokens
40,594 Tokens × ¥23/百万 Tokens = ¥0.933662
¥0.933662 ÷ 6.82 = $0.136900586510
¥0.933662 ÷ ¥0.933662 = 100.0000%
```

平台 quota 只能取整数，因此最终 `68,450 quota` 折算金额与未取整的表达式金额存在不足一个 quota 的
差异；Token 数、单价、表达式计算结果和墨行实际账单完全一致。

### 3.4. 墨行上游账单

| 模型产品 | Token 用量 | 计算参数 | 调用耗时 | 实际扣费 |
| --- | ---: | ---: | ---: | ---: |
| `doubao-seedance-2.0-mini` | 输入：`0`<br>输出：`40,594`<br>总计：`40,594` | `4 秒` | `134.93 s` | `¥0.933662` |

上游金额精确满足 `40,594 × ¥23/百万 Tokens = ¥0.933662`，说明本次墨行 Mini 按输出
Token 结算。再次直查终态仍为输入 `0`、输出 `40,594`、总计 `40,594 Tokens`，与首次终态和账单
记录完全一致；按本次历史账单金额反推，实际单价与火山国内无视频输入官方原价一致，没有额外折扣。

### 3.5. 阶段性结论

1. 渠道 67 的 Seedance 2.0 Mini、480p、4 秒文生视频请求已成功完成，生成结果可正常获取。
2. 墨行首次终态、再次直查终态和账单记录均为输入 `0`、输出 `40,594`、总计 `40,594 Tokens`；
   Token 数量已完成三方复核，没有差异。
3. 平台已使用实际输出 Token 完成结算：预扣 `546,334 quota`，最终扣除 `68,450 quota`，退回
   `477,884 quota`，证明实际用量差额结算链路已经生效。
4. **本次客户账单与 Provider 账单均可以对上：**我方和墨行均按 `¥23/百万 Tokens` 计算，实际金额
   `¥0.933662`，与火山官方原价一致。
5. 本次请求没有传入 `seed` 或 `camera_fixed`，与当前墨行 Mini 参数合同一致；公开响应内部字段隔离
   仍需在服务重启后的新任务中单独验收。
