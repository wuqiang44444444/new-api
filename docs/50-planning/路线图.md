---
status: current
owner: Dev Team
last-reviewed: 2026-07-28
---

# 路线图

> 更新基线：`main@cf39b06e`（2026-07-28）。勾选表示代码与确定性回归已经落地，不等同于渠道已加入全部生产分组；真实上游、账单和灰度门禁单独保留为未完成项。

## 当前状态摘要

- **已交付：** API Key 用量账单；统一素材库与素材感知路由；统一图片接口、共享异步 Task 与按结果结算；Seedance、Kling、即梦官方北向合同及官方/反代/中转南向适配。
- **当前收口：** Moxing / Qihang 图片真实渠道验收；视频合同重构后的多 profile 灰度、计费复核与旧入口退出。
- **下一阶段：** 账单详情补齐；内置 API 文档中心；Stable Diffusion 类图片渠道。

## 统一图片渠道与异步任务生产验收

背景：TokenSave / Moxing 等上游通过统一媒体任务协议提供图片生成能力，现有 Gemini 和 Advanced Custom 渠道仅靠原生转发无法处理 HTTP 202、任务轮询和 OpenAI 图片响应归一化。

- [x] 在 Advanced Custom 渠道中增加供应商无关的南向执行策略 `media_task_image_blocking`，不新增渠道类型，也不注册为通用请求格式 converter。
- [x] 北向保持 `POST /v1/images/generations`；南向优先调用渠道配置的图片入口，HTTP 200 复用现有响应链路，HTTP 202 轮询 `GET /v1/media/tasks/:task_id`。
- [x] 将成功结果中的 `result.primary_url` 或 `result.urls` 转换为 OpenAI 图片 `data[]`；任务失败、坏响应和超时均禁止重新创建任务。
- [x] 为 usage Gemini 3.1 Flash Image、Seedream 4.5 和 Seedream 5.0 增加尺寸、数量、参考图、画幅及扩展字段边界，并提供模型感知的渠道测试请求。
- [x] 阶段 C 收口为仅支持 `response_format=url`；固定尺寸价 `Gemini-3.1-Flash-Image-Preview` 不进入模板，直接请求及模型映射命中时均在发送上游前拒绝。
- [x] Seedream 使用系统 `ModelPrice` 和实际图片数量结算，不增加尺寸倍率、供应商价格表或专用价格常量。
- [x] 增加配置、请求转换、轮询成功、任务失败、超时、SSRF 防护和重复提交保护回归测试。
- [x] 阶段 D 复用共享 `Task` 完成图片任务持久化、后台接管、终态 CAS、固定按张差额结算、失败/过期退款和计费补偿；未新增图片专用任务表或第二套状态机。
- [x] 为长任务增加本站 HTTP 202、`GET /v1/images/tasks/:task_id`、所有权校验和 `Idempotency-Key` 恢复合同；同步 HTTP 200 路径保持原合同。
- [x] 为 `media_image` 增加 30 分钟生命周期保护下限；上游去重结果数超过请求 `n` 时按协议违规失败退款，不向用户补扣超发图片。
- [x] 表达式计费在创建前按管理员配置的任务 Token 上界进行保守预扣，终态可使用经过边界校验的完整图片 usage；usage 缺失时保持安全预扣上界，不按尺寸推断。
- [ ] 使用真实 Moxing 渠道完成 `seedream-5-moxing`、`nano-banana-2` 的 HTTP 200/202 联调，并使用真实 Qihang 渠道复核 `seedream-5-qihang` 同步链路；核对预扣、实际图片数量结算和失败退款日志。
- [ ] 使用真实 usage 响应和上游账单核对 `nano-banana-2` 的文本输入、图片输入、图片输出 Token 语义；核对通过后再启用 `tiered_expr`，未通过时继续使用本站 `ModelPrice`。
- [ ] TokenSave 如需发布，建立独立公开 SKU，并重新完成能力、模型映射、同步/异步响应和账单验收；不得直接复用 Moxing 的公开 SKU。

## Seedance 多上游视频合同收口与灰度

背景：Seedance 当前北向合同是 ModelArk v3 `POST /api/v3/contents/generations/tasks`；Moxing 反代和中转属于南向渠道差异，不再建立 Moxing 专用渠道类型或要求客户端使用 `/v1/video/generations`、`/v1/videos` 创建任务。2026-07-17 的三渠道 720p/5s 实测已验证 usage 与差额结算；2026-07-28 视频合同重构后仍需按当前公开 SKU 和渠道配置重新灰度。

### 已完成

- [x] 复用 `DoubaoVideo` 和共享 Task，以 `official`、`third_party_reverse_proxy`、`third_party_relay` 三个供应商无关 profile 隔离南向协议。
- [x] 第三方反代使用可配置 `/v1/ark/media/generations` 与 `/v1/ark/media/tasks/{task_id}`；第三方中转使用可配置 `/v1/media/generations` 与 `/v1/media/tasks/{task_id}`。
- [x] 渠道 Base URL、创建路径、查询模板和 `{task_id}` 占位符在保存及调用前校验；Base URL 使用站点根地址，避免重复 `/v1`。
- [x] 公开模型、上游模型与计费模型解耦，模型映射在发送上游前完成，并将公开模型和上游模型冻结到任务事实。
- [x] 中转转换支持文生、首帧、首尾帧和参考图输入；`duration`、`ratio`、`generate_audio` 分别转换为 `duration_seconds`、`aspect_ratio`、`with_audio`，并保留显式 `0` / `false`。
- [x] 当前中转 profile 对未实现的视频/音频参考输入和冲突组合明确返回错误，不再静默删除或降级字段。
- [x] 创建响应、四态状态、`result.primary_url` / `result.urls`、错误与可信 usage 已归一化到内部任务合同；未知状态和成功无结果 URL 均 fail closed。
- [x] 本地任务 ID、上游任务 ID、请求 ID、北向合同版本、南向 profile、查询地址/模板、实际 Key 和计费事实均进入任务快照，后台轮询、视频代理、结算和补偿复用现有主链路。
- [x] 已有请求转换、显式零值、创建响应、状态、结果 URL、路径校验、任务快照、三 profile 端到端结算与退款等确定性回归测试。

### 待完成

- [ ] 按当前 ModelArk 北向合同，对 `official`、`third_party_reverse_proxy`、`third_party_relay` 各完成至少一条真实创建至终态任务；覆盖已发布 SKU 支持的文生、图生、首尾帧和参考图组合。
- [ ] 在真实上游验证创建失败、查询失败、任务失败、超时、重复轮询和幂等恢复，核对只发生一次结算或退款且额度不为负。
- [ ] 逐渠道复核 ability、调用分组、模型映射、可用分辨率/时长、`service_tier` 和计费表达式；用真实 usage 与供应商账单重新核对视频输入、分辨率和时长档位。
- [ ] 确认普通用户响应不暴露上游任务 ID、真实模型、渠道和结果源地址，并能通过本站视频代理取得最终内容。
- [ ] 完成 OpenAI 视频存量只读流量、旧入口和兼容别名的退出统计与下线；不得恢复 OpenAI 视频创建。

## 账单详情增强：长短 context、视频信息与缓存命中

背景：日志扩展信息（`Log.Other` JSON）对文本请求的缓存命中记录已较完整（Anthropic / OpenAI / Gemini / Moonshot / Llama.cpp 的 cache 字段均已解析归一化并在详情弹窗展示），但存在三类缺口——长短 context 仅在 tiered 表达式计费时通过 `matched_tier` 可识别、视频任务日志几乎不含可结算的视频元数据、后端已写入的 `cache_write_tokens` 与 `input_tokens_total` 未在前端声明和展示。本条目在不新增数据库列、不破坏 SQLite/MySQL/PostgreSQL 兼容的前提下补齐这些账单详情。

### 缓存命中（补齐展示缺口）

- [ ] 在前端 `web/src/features/usage-logs/types.ts` 的 `LogOtherData` 中补声明 `cache_write_tokens` 与 `input_tokens_total`，对齐后端 `service/text_quota.go` 已写入的字段。
- [ ] 详情弹窗 TokenBreakdown 与表格 Tokens cell 改为直接消费后端归一化的 `cache_write_tokens`，移除前端用 `cache_creation_tokens_5m + cache_creation_tokens_1h` 自行重算的逻辑，避免与后端 fallback 不一致。
- [ ] 详情弹窗展示 `input_tokens_total`（上游真实总输入），便于与 prompt_tokens、缓存命中三方核对。
- [ ] 增加前端回归测试覆盖按 `cache_write_tokens` 展示与字段缺失时的 fallback 契约。

### 长短 context

- [ ] 在日志 Other 中补齐长短 context 判定，使非 tiered_expr 计费的请求也能看到 context 归属；判定基于已结算的 token 数与可配置阈值，不得引入新的计费路径或额度变化。
- [ ] 将 `pkg/billingexpr` 已计算的 `CrossedTier`（是否命中 tier 边界）写入日志 Other，供 tiered_expr 与通用分级共用。
- [ ] 详情弹窗展示 context 归属标签（如 `short` / `long`）与阈值来源；标签为只读展示，不参与额度计算。
- [ ] 若需要按模型 context window 自动分级，阈值来源限定为配置或现有 model 元数据读取，禁止在请求热路径新增数据库查询，保持最小入侵。

### 视频信息

- [ ] 视频任务消费日志（`service/task_billing.go::LogTaskConsumption` 与 `taskBillingOther`）的 Other 中补齐可结算视频元数据：时长（秒）、分辨率、画幅；取自已通过边界校验的计费输入，禁止写入未校验的上游字段。
- [ ] 从上游轮询响应解析的视频元数据（参考 `dto/video.go` 的 `VideoTaskMetadata{Duration,Fps,Width,Height,Seed}`）在写入 `Task.Data` 时同步抽取为结构化字段，供日志与任务表格读取；来自 upstream 或媒体头的时长必须经过饱和转换，不得直接作为计费倍率。
- [ ] 任务日志表格 `web/src/features/usage-logs/components/columns/task-logs-columns.tsx` 增加视频时长、分辨率、画幅列与详情入口，并区分墙钟 Duration（已有）与视频内容时长。
- [ ] 视频地址（`Task.PrivateData.ResultURL`，当前为私有）在任务日志中的展示遵循现有私有字段策略：仅对有权限的视图暴露，且不回写进通用日志 Other。
- [ ] 增加后端回归测试覆盖视频元数据写入 Other 的边界校验与字段缺失时的 fallback。

## 内置 API 文档中心

状态：目标架构已经接受，但当前只有 `/api/status` 的 `docs_link` 与顶部导航 `/docs` 回退；尚无 `/docs` 页面、公开 Markdown 内容、manifest、安全渲染或搜索实现。实施以[内置 API 文档中心架构](../20-architecture/内置API文档中心架构.md)为边界。

- [ ] 审计 `docs/openapi/relay.json` 与当前图片、视频、素材及文本北向合同的差异，明确可公开端点；后台 `docs/openapi/api.json` 永不进入公开产物。
- [ ] 在 `web/public/docs-content/` 建立受控 `manifest.json` 和首批中文 Markdown，覆盖快速开始、鉴权、错误、图片、视频、素材与任务查询；示例只使用占位 Key 和动态 Base URL。
- [ ] 在独立的 `web/src/features/docs/` 与 `web/src/routes/docs/` 实现公开路由、导航、正文、本页目录、代码复制和移动端布局，不扩写现有通用 Markdown 组件。
- [ ] 对 Markdown 执行 manifest 白名单解析和 HTML 净化，禁止任意路径、脚本、iframe、表单及真实凭据自动填充。
- [ ] 接入当前站点状态与请求 origin，统一首页、顶部导航和文档页的链接回退；外部 `docs_link` 配置继续优先。
- [ ] 增加 manifest、路径安全、净化、动态变量、路由刷新和构建产物测试，并通过前端 typecheck、lint、format、测试与生产构建。
- [ ] 上线前完成人工公开面审查，确认未暴露 Provider 私有字段、渠道配置、管理 API、内部 Task 数据或敏感示例。

## 增加 Stable Diffusion 类图像渠道

背景：图像生成已通过统一图像接口（`POST /v1/images/generations`，commit ac39131a）标准化，但仓库内尚无任何 Stable Diffusion 渠道——无 Stability AI / SD WebUI(A1111) / ComfyUI 的渠道类型、常量、DTO 与适配器，唯一痕迹是 `relay/channel/siliconflow` 中被注释掉的若干 `stabilityai/*` 模型名。本条目按已固化的「模式 B」接入路径补齐 SD 类渠道，复用统一图像接口与现有按张计费，不改主链路与计费链路。

### 渠道与适配器

- [ ] 新增 Stability AI Stable Image API（`/v2beta/stable-image/generate/*`）渠道：在 `constant/channel.go`、`constant/api_type.go`、`common/api_type.go` 注册新 ChannelType/APIType（当前最大 `ChannelTypeAdvancedCustom = 58`，新增取 59 起），并在 `relay/relay_adaptor.go::GetAdaptor` 接线。
- [ ] 新增 SD WebUI(A1111) 兼容渠道（`/sdapi/v1/txt2img`、`/sdapi/v1/img2img`），覆盖自建 SD 部署；按相同的模式 B 注册并接线。
- [ ] 各 SD 渠道实现 `channel.Adaptor` 的 `ConvertImageRequest` 与 `DoResponse`；DTO 参考 `relay/channel/siliconflow/dto.go` 的 `SFImageRequest`（已具备 negative_prompt、num_inference_steps、guidance_scale、多图输入等 SD 风格字段）。
- [ ] `DoResponse` 将上游返回归一化为 OpenAI 图像 `data[]`（base64 或 URL），复用 `OpenaiImageHandler` 解析路径。

### 模型识别与计费

- [ ] 扩展 `common/model.go` 的 `ImageGenerationModels`，使 SD 系列模型名被 `IsImageGenerationModel` 识别，确保端点路由把 `EndpointTypeImageGeneration` 排到首位。
- [ ] 计费统一走系统 `ModelPrice` × 实际图片数量，不新增尺寸/步数等专用倍率或专用价格常量；图片数量 `n` 复用 `dto.MaxImageN` 上界，进入额度计算前完成边界校验。
- [ ] 上游返回图片数量与请求 `n` 不一致时，沿用既有「按实际交付数量结算、超发去重按协议违规失败退款」合同，不向用户补扣超发图片。

### 验收

- [ ] Stability AI 与 SD WebUI 两类渠道分别完成 txt2img / img2img 至少一条真实任务，并核对预扣、按张结算与失败退款。
- [ ] 增加请求转换、响应归一化、按张计费边界与模型识别的后端回归测试。
