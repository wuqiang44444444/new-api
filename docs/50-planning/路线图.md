---
status: current
owner: Dev Team
last-reviewed: 2026-09-23
---

# 路线图

本文只记录尚未完成、仍会影响发布或后续开发的事项。已经落地的产品和架构事实进入 `10-product/`
与 `20-architecture/`，正式变化进入[变更记录](变更记录.md)。

1. 移动云素材库需要从 HK 出口，美国站不支持
2. seedance 渠道保存的时候有问题
3. Seedance Channel 默认素材组的真实 Provider 与生产灰度验收尚未完成。代码、公开元数据与
   本地回归已完成；发布前须对每个准备开放的 `default_fallback` 协议验证管理员创建或复用、
   未传/空/空白组 ID 使用 Channel 默认组、显式非空 ID 原样优先且 Provider 拒绝后不回退、
   默认组缺失返回稳定 409、敏感输出脱敏，并记录重启持久化与单 Channel 灰度结果。验收完成前
   不得宣称所有线路已生产可用。
4. 统一图片服务（2026-09-05 实施 + 同日审计修复轮）未完成项分两类。待实现：Google quality/
   格式压缩/透明背景/moderation/input_fidelity 的南向映射与后处理、mask 真实遮罩编辑、同步 SSE
   partial_images、file_id 解析授权、部分成功逐图错误归因、精确像素输出转换表（依赖 G4 实测）。
   图片中转 edits/参考图已实现，仍需补齐逐模型与输入图/失败/超时计费证据。
   生产取证：真实 99/103 验证 URL 直传/MIME/区域与
   14 张 URL（G2）、20/50 MiB 与 Vertex inline 实测（G3）、参数真实效果（G4）、file_id（G5）、
   B1—B10 业务闭环（G11）、两节点容量/背压实测与默认值冻结（G12）、Azure S3 网关 presign 灰度
   （G9）。完成度口径本条清单与
   [图片服务架构](../20-architecture/图片服务与异步Provider适配架构.md) §0 为准（历史三分表见
   [归档的实施计划](../99-archive/2026/09/2026-09-05-统一图片服务实施计划.md) §5）；
   全部完成前异步模式与 gemini_image 族不得宣称生产可用。
5. 图片计费闭环修复（真实验收 2026-09-06 发现的未通过项）：FunCloud 图片渠道 `writeImageResponse`
   返回空 usage，按 `img_o` 收费的表达式实际扣款为 0，须按已核实的 Provider 计量配置可履约价格；
   账单 `statement_snapshot` 在表达式信息注入前形成、模式只记 token，须改用结算时冻结的计费模式与
   表达式；图片用量日志须保存 `img_o` 等图片分项，使表达式费用可独立复算。修复后须经日志实际
   复算验证，不得用假 Token 或静默按次降级冒充通过。
6. `generate_audio=false` 的成品仍含非静音音轨（2026-09-06 实测）。网关北向/南向证据均确认按
   `false` 传递，须携带内部证据向对应 Provider 核查该参数的支持语义与实现；核查完成前不得对外
   承诺静音效果，也不自动转码去音频或改变统一北向协议。
7. 渠道补验：官方 Google 图片账户月度额度（当前 429）补齐后复验；Vertex 渠道发布 imagine 图片
   模型后复验；JD 渠道（库内仍启用但 Provider 对标准请求返回 400）由配置负责人核对映射与状态；
   S3 真实云调用与音频生成/转写真实证据待配置后补验。各项完成前对应模型不得标为生产可用。
8. 音视频证据二期未接入：Realtime/WebSocket 双向流、Volcengine TTS 与讯飞独立 WebSocket、
   Midjourney 自有发送路径、Chat/Responses/Gemini 媒体入口按需开启。二期完成并通过覆盖矩阵验收前，
   不得宣称全量音视频请求记录。
9. 模型广场未展示 Seedance 性能指标：异步视频任务的耗时/成功率/用量指标未接入采集，平台不得
   把视频任务耗时伪装成文本 TPS，也不得用模拟或无指标数据填充模型广场；如需展示须先定义视频
   任务指标合同（受理到完成耗时、成功率等）并完成真实采集验证。
10. FunCloud V3 与托管素材真实 Provider 验收未完成：真人模式固定字段的首次真实成功生成、内部签名
    有效期与真实取图窗口核对、对象存储真实部署验收、托管验收矩阵逐项执行、公开 API 文档与部署说明
    均未执行；Mini 素材引用生成侧不可见需供应商核验账号素材域与生成路由；V3 参考视频组合实测、
    供应商账单逐任务核对、MySQL/PostgreSQL 升级与并发验收、`funcloud_material` 单素材删除真实北向
    生命周期验收（ark_assets_v1 / byteplus_action 待有在库渠道后补实测）仍在等待。各项完成前对应
    能力不得宣称生产可用。
11. 视频任务错误详情公开归一已完成本地实现与回归；生产部署与全部已发布视频协议的真实线上错误矩阵
    验收未执行。完成前不得宣称所有视频入口的详细错误展示已通过真实验收。
12. Azure Batch（代码已完成）：真实 Azure 验收（部署/SKU/API 版本、创建/取消/部分结果、usage 与
    缓存语义、上游费用核对、SDK 行为）、Batch 表在 MySQL/PostgreSQL 的实机迁移与断点重入、
    10,000 行/长单行/超 20MiB 结果的容量演练与归集中断恢复均未执行。迁移前须完成数据库备份与
    离线预检、多候选渠道映射决策。
13. 多合同存量迁移：生产库的迁移预检、备份与多候选渠道映射决策未执行（迁移预览/执行/批量入口
    代码已完成）。执行前不得宣称存量合同已迁移。
14. Seedance 计费统一与官方标价：本地库 28 个客户模型已全部迁移为 u() 美元表达式并完成官网核价；
    运行节点加载新代码/配置、真实 Provider 账单、外部 MySQL/PostgreSQL/ClickHouse 与多节点一致性
    验收未执行。日志交付表 `task_billing_deliveries` 需随下次部署正常迁移。
15. 未知持款处置：173 条 `unknown/held` 创建记录（约 1.43 亿 quota，3 个用户）均无上游任务 ID，
    仅 1 条可关联到 HTTP 503 上游响应（不能证明未创建）；另有 3 条已释放客户资金但保留 Provider
    exposure。须取得可信 Provider 证据或由业务明确决定承担潜在成本后释放；处置前不自动退款、
    不重发、不改状态清零。
16. 历史账单关联缺口：457 条初始日志未建立可验证的完整任务归属（441 条缺唯一冻结请求身份、16 条
    缺唯一已转移预扣证明）；另有 291 个历史 Task 未找到带同 Task ID 的日志。逐项核实或如实保留
    未知，不得按金额/时间猜测补写，也不得把汇总相等写成逐任务勾稽完成。
17. Synlink 剩余验收：新版插件 `1.3.5`（字符串／对象型可信失败终态与分类诊断）的发布、冻结版本隔离与各运行节点
    实际加载核对未完成；旧版本待核对任务须逐笔关联失败证据、核实资金后按
    [处置手册](../40-operations/Synlink失败任务处置手册.md)处理，不自动改冻结快照；
    其它三个模型生成、URL+托管混合输入、processing/失败/不存在/鉴权与配额失败
    的真实响应分类、明确签名到期提示的真实交付验收、尾帧鉴权与二进制交付、缺用量恢复与供应商货币账单；
    视频仅临时下载，不建设长期托管或自动签名刷新；查询 404 继续待核对，不立即退款。官网声明
    `/api/v3/contents/generations/tasks` 与本地登记旧路由的差异须真实 Provider 验收后处理。
18. 墨行 0818 验收：`watermark=false` 最终画面、同租户国内素材消费、四模型媒体组合与时长/费用
    真实账单尚未完成；旧 2.0（无后缀 ID）的 watermark 异步拒绝问题维持记录，不迁移到新模型结论。
19. Seedance 上游同步 U4：保留上游独立新增的三方合并全树演练与回归未完成；现有证据只有配置合同
    局部重放和“重建当前实现”验收，不宣称合并成本已验收。
20. 表达式展示投影与管理员试算（`billing_display`、`/api/option/billing-expression/preview`）：
    代码与本地回归完成，线上部署、生产表达式与真实消费/账单核对未执行；完成前不宣称线上分时
    展示已验收。
21. 视频请求证据：新增证据错误分类的部署核验、原始报错请求的实际根因定位、客户对新增错误码的
    兼容确认未完成；不能仅凭分类测试通过宣布原线上故障已定位。
22. GPT 聊天参数兼容：`gpt-chat-latest` 固定名称登记与能力表已实现；真实 Azure 官方渠道的定位、
    非流式/流式调用与 usage/账单核对未完成。
23. 待决策：FunCloud 未发布字段（`callback_url`、`service_tier`、`execution_expires_after`、
    `draft`、`tools`、`safety_identifier`、`frames`）与 Synlink 回调的统一合同需先定范围再实现；
    Batch 性能优化（迁移预览与 DashboardListModels 查询扇出、输入多遍解析合并）按实测决定优先级；
    Doubao 渠道迁移维持用户暂缓结论，重启须重新核对模型、合同、在途资金与素材；上游 URL 汇总
    如需用于正式供应商结算，请求发生时的基础 URL 快照须另行评审实施，不用当前配置回写历史。
24. 状态核对：用户生命周期邮件生产验收与存量无邮箱提示、对账能力的外部数据库验证、公开文档中心
    的视口/缓存人工验收，须先核对历史关闭依据再决定是否补验收；不重开已关闭的旧主题。
25. 线上定价配置核对与迁移发布（2026-09-14 只读核对遗留）：六项费率/缺价疑点（`seedance-2.0-cmcc`、
    `seedream-5-pro`、`deepseek-v4-flash-0731` 与 `deepseek-v4-pro-0813` 的缓存倍率、
    `seedance-2-0-fast-s` 同式复制、`gemini-3-pro-image-preview` 与 `gemini-3-pro-image` 的 10 倍
    差异）须管理员逐项给出有依据结论，不按名称或默认价推断；线上 28 个旧 `_task` 表达式模型须
    先核对部署版本与配置基线，再按 18 个可证明转换批次与 10 个 `size_multiplier` 人工核验分批
    迁移，等价迁移与主动调价分开记录；受控发布、缓存失效、在途任务保护与回退条件未执行。完成前
    不宣称线上定价展示与计费配置已收敛。
26. 前端通用任务状态映射缺 `CANCELLED`、`EXPIRED`、`PROVIDER_CONTRACT_FAILURE` 的本地化标签
    （未映射状态回退为原始枚举文本与中性徽章）；补齐前任务终态在用量日志的呈现不完整。
27. gpt-image-2 渠道实测遗留（2026-09-14）：渠道 48 未兑现请求尺寸与 `n>1`、渠道 102 上游直接
    少图均已直连复现，须向供应商提供脱敏对照并按响应格式/参数分开验收；102 此前 API 与图片下载
    403 的产生位置未定位；消费日志「生成数量」记录请求 `n` 而非实际交付数，须区分请求事实与
    交付事实后再评估结算可观测性；本机 CPU 过载拦截与保护阈值排查未做；渠道 57 仅完成部分
    尺寸×数量×质量组合复测，不据此承诺全组合履约。
28. 2026-09-19 归档批次：`80-dev` 两批共 34 份方案/核验/评审记录已归档至 `99-archive/2026/09/`。
    原跟进入口[80 目录未完成事项汇总](../99-archive/2026/09/2026-09-19-80目录未完成事项汇总.md)
    已于 2026-09-23 归档，未完成与待裁决项由本条承载（逐项关闭前须对照归档件与当前代码复核）：
    - 生产级验收门槛（多方案共同声明，不得以本地/SQLite 结果替代）：MySQL 5.7.8 精确版本实测、
      真实三库并发、OSS 备份恢复演练、ClickHouse 实库、多实例灰度、来源修订钩子目标库延迟基线、
      `repair-task-billing-logs` 生产一致性副本复演（PG 9.6.24、ClickHouse 指定用例与真实 SMTP
      收件已本地完成）。
    - 待决策：error_events 请求体保留/截断/按模块关闭及清理排期；邮件报告"无调用"状态补实现或
      确认降级；randy $8.388890 残差与全用户累计校准待裁决后另行立项；历史"图片折扣=1"独立核查
      （不自动退款、不改历史账单）。
    - 账单与对账域：生产三库并发/恢复演练/多实例灰度；部署级三类计数核验与全历史退款候选扫描预算；
      账单确认上线前置（关闭确认、全部实例与前端同升、生产实际数据重核验，减免/退款/重复排除的
      财务执行流程未建立）；66 笔补正生产快照重验与逐组裁决；Responses/多段转换/缓存写入扩展解析
      另行实施；附加费单位换算证明。
    - 错误日志与运维域：4xx dashboard/插件分类与受控部署验收；error_events TTL/清理排期与单 worker
      队头出口隔离；渠道测试错误日志实操验收；小时报告调度与生产灰度；存量证据密钥恢复；索引 DDL
      生产部署窗口与上线核对；743ms/281ms 慢查询未复现先定位（Casbin 慢查询须原进程复现定位，
      定位前不改索引、不加缓存、不动阈值）；低优先跟进清单见归档汇总。
    - 合同域：生产存量合同/Key 只读盘点（失效组合、未列模型流量、权限冲突、跨组重试开关、跨组价差）、
      生产部署、真实 Provider/账单与灰度验收；升级前私有合同组配置治理；`/v1/models` 与
      `/api/pricing` 合同范围收窄投影验收确认；Seedance 等类型化入口组合失效的失败语义；跨组重试
      预扣调整规则升格为显式验收项。
    - 图片域（Gemini Lite）：auto 默认输出像素、Gemini 24 真实生成/编辑、非正方形输出、Google 正式
      账单核对、其他渠道/别名真实调用、生产灰度与延迟分段测量；旧任意 WxH 语义待执行任务处理或排空；
      "确认价格配置存在且口径正确"固化为显式验收动作。
    - 渠道自动检查 §3.6 第 4 步（最近业务生成结果展示、配置变化关联、长期未验证提醒、重复告警聚合）、
      #109 渠道配置真实根因、各 Provider 只读探针操作范围/凭据/费用/限流核实、严格成本封顶验收、
      生产灰度。
29. 2026-09-23 归档批次（15 份已归档至 `99-archive/2026/09/`；原
    [80目录方案验收清单](../99-archive/2026/09/2026-09-23-80目录方案验收清单.md)已归档，本条为剩余
    部署/生产/业务验收的唯一跟进入口。验收绑定代码版本、环境与固定数据截止范围；先只读盘点与界面
    验证，再由部署负责人安排灰度与资金处置窗口；不把构建、单元测试或隔离环境验证当生产发布）：
    - 日周统计与评审修复（[日周统计](../99-archive/2026/09/2026-09-20-客户与上游日周用量统计实施方案.md)、
      [评审修复记录](../99-archive/2026/09/2026-09-22-80目录评审问题修复记录.md)）：目标部署验证正常流、
      失败、取消与重试去重；单日/自然周的页面、客户总览和新 CSV 一致，已记录用量/金额不变；同范围
      容量验证不超时、不静默截断。
    - 合同模板与模型多选（[合同模板](../80-dev/2026-09-18-客户合同模板功能分析与实施方案.md)、
      [模型多选](../99-archive/2026/09/2026-09-23-客户合同添加模型多选分析与方案.md)）：目标数据库升级
      及存量合同验证；中英文、窄屏、长名称、多模型、各模型不同渠道、版本冲突保留草稿；渠道停用/删除/
      模型移除/分组删除均可定位，客户视图不泄露内部原因。
    - 对账合并、月度折扣、名称与渠道展开（[合并与折扣](../99-archive/2026/09/2026-09-20-上游对账合并与渠道月度折扣方案.md)、
      [名称与展开](../99-archive/2026/09/2026-09-20-上游对账名称与渠道展开调整方案.md)）：目标数据库
      升级、历史折扣冲突清单和并发保存；页面/导出同截止范围勾稽，真实对象存储交付成功；需要
      ClickHouse 的部署单独验收其已支持读取边界。
    - 全层级分页（[分页实施](../99-archive/2026/09/2026-09-21-上游对账全层级分页实施.md)）：目标规模
      账期测量冷缓存耗时、峰值内存、并发和超时；确认分页前后全量汇总不变；分页不免除全账期扫描，
      未取得测量前不承诺容量改善幅度。
    - 渠道测试费用（[费用方案](../99-archive/2026/09/2026-09-21-渠道测试费用纳入上游汇总分析与方案.md)、
      [历史核查](../99-archive/2026/09/2026-09-21-历史渠道测试未纳入金额原因核查.md)）：固定同一数据
      截止范围复核已计价/待核算及互斥原因，解释预扣排除、任务去重、恢复与重分类影响；真实账单抽样
      完成，未知金额不按现价补算。
    - 媒体连接探测（[探测优化](../99-archive/2026/09/2026-09-19-图片视频连接探测运维口径与优化.md)）：
      部署后复核媒体检查无生成副作用；历史标签与未验证提示页面可读；真实失败分支覆盖逐项留证，
      不把未发生的错误分支算已验收。
    - 视频占款退款（[占款闭环](../99-archive/2026/09/2026-09-22-视频创建未知结果长期占款问题分析与退款闭环方案.md)）：
      更新生产只读清单和备份，核对原报告 199 条及其他超期状态；通过正式入口分批处置，核销 held/
      已转移/已退/待处理的数量与金额；钱包和 Key 分维度、重复执行不多退、迟到成功不补扣，失败项
      仍可追踪；完成灰度。
30. 近三天错误分析剩余事项（[主分析](../99-archive/2026/09/2026-09-22-近三天数据库错误分析与代码改进建议.md)
    已归档；已实施 4 项的当前行为见 [运维-API错误日志架构](../20-architecture/运维-API错误日志架构.md)）：
    部署后验证合同拒绝、渠道状态变更、素材诊断和 Gemini 结构诊断可关联且脱敏（真实 MySQL/PostgreSQL
    与生产灰度未执行）；Gemini 结构诊断最小复现待真实失败样本；跨夜持续运行（服务时段持续可用、任务
    正常续租、异常退出可恢复）运维处理；P2 取消流用量/失败/交付事实观测待实施，收费政策在 Provider
    实际成本与应收依据可核实前暂缓；加密上下文待补证据；图片尺寸与素材源访问按调用方/源站验证，
    不作为已确认代码 bug；图片待核实任务可观测性未启动；429 及两笔关联任务维持用户排除结论，
    恢复范围由用户明确。
31. 计费表达式全局美元汇率（设计未实施，方案见
    [实施方案](../80-dev/2026-09-23-计费表达式引用全局美元汇率分析与方案.md)）：完成求值入口
    与多实例传播核查后独立实施；汇率函数、冻结字段、各执行入口接线、设置校验增强、展示投影和缓存
    更新均未实施，实施前还须核查全局设置跨节点同步、全部求值/快照调用点、现有函数白名单与保存校验
    覆盖、原生文件必要改动清单及发布/回滚在途任务兼容性。未实施前模型表达式不得引用该函数，
    不宣称 USD 官网标价展示已支持。
