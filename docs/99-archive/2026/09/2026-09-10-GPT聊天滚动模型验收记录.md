---
status: historical
owner: Dev Team
last-reviewed: 2026-09-10
archived-at: 2026-09-14
source-path: docs/80-dev/2026-09-10-GPT聊天滚动模型验收记录.md
superseded-by:
  - docs/40-operations/09-Azure-GPT-5.6-Chat与Responses兼容运维手册.md
  - docs/50-planning/路线图.md
---

# GPT 聊天滚动模型验收记录

**2026-09-10 复验补记：** 当前根模块完整构建及后端全量测试已经通过，原未定义符号阻塞已解除，见[联合评审修复与复验](2026-09-10-多方案联合评审问题修复与验证记录.md)。下文保留初次验收快照；真实 Azure 调用与部署验收仍未完成。

## 问题与目标

验收[固定名称兼容修复](./2026-09-10-GPT聊天滚动模型固定名称兼容修复.md)，确认
`gpt-chat-latest` 按当前 GPT-5.6 参数规则处理。用户指定真实验收目标为 Azure 官方渠道。

**初次验收结论（历史快照）：暂不通过整体验收。** 当时独立模块验证及 Azure 模拟请求通过，但根模块编译失败，
且尚未定位具体真实渠道，不能以隔离副本的模拟结果代替当前工作区或真实 Azure 验收。

## 当前实际情况

环境为当前工作区未提交源码，HEAD `3f7e6f2a6`，Go 1.26.5。

| 验收项 | 本次结果 | 范围 |
| --- | --- | --- |
| `cd relaykit && GOWORK=off go test ./dto -count=1` | 通过 | 当前工作区；覆盖精确名称、能力一致性与名称边界 |
| `cd relaykit && GOWORK=off go build ./...` | 通过 | 当前工作区独立模块 |
| 根模块 `GOWORK=off go build ./...` | 失败 | 当前工作区，存在下列两个未定义符号 |
| 当前工作区 OpenAI/Azure adapter 回归 | 无法启动 | 测试包构建被同一问题阻塞，不能记作测试通过 |
| Azure 专用渠道 12 项模拟调用 | 通过 | 已验证 rc36 隔离副本通过 overlay 加载当前修复；不代表当前根模块构建通过 |
| 真实 Azure 官方渠道 | 待执行 | 仅收到渠道类型，尚缺网关地址及渠道 ID/完整名称 |

### 当前工作区阻塞

- `pkg/publicmodel/video.go:162` 仍引用已不存在的 `dto.VideoUpstreamProtocolMoxingMediaTaskV1`。
- `relay/channel/task/seedance/assets/moxing_joycreator.go:32` 仍引用已不存在的
  `dto.AssetUpstreamProfileMoxingJoyCreator`。

这是素材协议删除过程中残留的生产代码引用，根模块构建与测试编译均可复现；本次没有恢复已删除协议、
新增兼容常量或改动其它任务的协议删除范围。

### Azure 专用渠道模拟范围

使用真实 `ChannelTypeAzure` 和 `testChannel`，临时 SQLite 合成用户及拦截式 HTTP transport。
测试三种客户名称：`gpt-chat-latest` 直用、`chat-latest` 映射、`customer-chat` 映射；
均以 `gpt-chat-latest` 为最终名称。每种组合覆盖自动/显式 Chat 端点、流式/非流式，共 12 项。

每次均确认：

- 路径为 `/openai/deployments/gpt-chat-latest/chat/completions`。
- `api-version` 保留测试配置值，`api-key` 头携带合成测试凭据；未使用或输出真实凭据。
- 请求 `model` 保持 `gpt-chat-latest`，`max_completion_tokens` 为 16，完全没有 `max_tokens`。
- 模拟上游若收到旧字段即返回参数 400；本次 12 项均获得模拟成功响应并通过实际响应解析。

本测试不证明真实 Azure 部署名、可用 API 版本、配额、生成质量或 16 token 推理预算足够。
也未改变透传、双非零字段和参数覆盖的既有边界。

日志位于 `/private/tmp/yuan-chat-token-check/`：`acceptance-root-build.log`、
`acceptance-adaptor.log`、`azure-acceptance.log`；隔离测试入口为 `azure-acceptance-overlay.json`。

## 优化方案与验收闸门

1. 素材协议清理完成后，重新运行当前根模块构建与 OpenAI/Azure 回归，不通过则不能进入部署验收。
2. 获取实际网关地址和渠道 ID/完整名称；先核对运行版本是否包含本次修复、最终模型/部署名是否匹配。
3. 在指定 Azure 官方渠道分别执行非流式与流式测试，核对响应、结束原因和脱敏用量，确认不再出现
   `max_tokens` 参数 400；仅“模型不存在”、认证错误或预算不足不算业务验收通过。
4. 实际调用完成前保持“真实渠道待验收”；当前未部署、未执行真实 Azure 请求。

内部文档检查：`task docs:check`、`task ai:check`。
