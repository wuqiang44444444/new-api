---
status: implemented-pending-acceptance
owner: Dev Team
last-reviewed: 2026-09-22
---

# 80 目录评审问题修复记录

## 问题与目标

修复文档评审发现的四处功能缺口，并使测试与当前事实文档一致。本次只修改本地统计、折扣读取和
视频资金查询投影及其界面，不调整原生 Relay、资金执行、Provider 请求或 Task 业务终态。

## 修复范围

| 问题 | 当前行为 | 回归证据 |
| --- | --- | --- |
| 售后退款未纳入日周统计 | 按稳定任务交付 ID 读取 `customer_refund`，包含原生空计费状态 Task；归属任务结束日，退款不新增调用。已退零净额不再显示客户金额待结算，未交付日志仍保留缺项 | `usage_analytics_refund_regression_test.go`；真实交付日志生成及三类 CSV 视图测试 |
| 已删除渠道页面与导出折扣不一致 | 页面、日周统计、缓存元数据与导出共用只读月度规则：当月优先；缺少当月时继承上月有效值，否则默认 1。当月冲突继续未知，读取不创建配置或审计 | `billing_discount_read_regression_test.go`；跨批次冻结快照测试 |
| 周视图缺少业务层级的逐日数据 | Key、客户模型、URL、上游模型和渠道各自显示七日及合计，渠道在周视图默认展开；复用既有逐日表格，全宽展示并允许局部横向滚动，单日不重复增加明细 | `weekly-breakdown.test.tsx`；中英文、明暗主题及窄屏样例检查 |
| 成功但无结果的视频未进入异常筛选 | 基于既有结果字段及原生历史结果字段，在分页前统一筛选、排序和汇总；迟到结果立即解除异常，无新增持久化状态 | `video_fund_query_cross_database_test.go`，SQLite、MySQL 5.7.44、PostgreSQL 9.6 实测 |

客户退款只影响客户资金投影，不把售后退款视为 Provider 冲正；上游计费尚未完成时原价继续未知。
上游读取规则与当前产品事实见[客户与上游对账架构](../20-architecture/账单计费-客户与上游对账架构.md)。

## 文档与测试维护

- 修正重新生成导出测试的请求选项预期，保留来源快照及端点选择断言。
- 架构概览按 ADR-0021 描述 24 小时客户资金保障，不再写成 unknown 永远不自动退款。
- 日周方案将已完成的费用与计量修正标为已实施，698 条旧待核查状态指向后续 593／38／67 分类结果。
- 视频占款方案状态改为已实施待验收，保留早期分析的历史语境，最新结论指向第 9 节。

## 验证与边界

- model、service、controller、router 全量测试通过；退款交付、日周 CSV、折扣及异常查询回归通过。
- 前端用量与对账相关 22 个测试文件、163 项测试通过；类型检查、相关 lint、生产构建通过。
- `task docs:check`、`task ai:check` 通过；生产构建包含公开文档和品牌产物校验。
- 三库验证使用隔离数据库与构造数据，没有修改生产数据或执行客户退款。
- 真实 Provider、目标部署灰度、历史资金处置及统计容量验收仍沿原方案执行；本记录不宣称生产发布。
