---
status: accepted
owner: Dev Team
last-reviewed: 2026-08-11
---

# 飞彩全模型与计费设计

## 1. 十个 Provider 模型

下表是当前 Provider 模型能力和证据目录，不是 Link SKU。管理员为准备上线的每一行配置独立客户模型。
飞彩只维护这一组当前模型，不保留 v1/v2/v3 配置族或带 `-feicai` 后缀的旧 Provider 模型。

| # | Provider 模型 | 固定分辨率 | duration | 图片 min–max | 音频 max | 视频 max | 计费模式 |
| ---: | --- | --- | --- | ---: | ---: | ---: | --- |
| 1 | `seedance-2.0-vip-720p-mini-azhw` | 720p | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 2 | `seedance2.0-sd2` | 720p | 11–15 | 1–9 | 0 | 0 | 按秒 |
| 3 | `seedance-2.0-vip-720p-fast-azhw` | 720p | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 4 | `seedance-2.0-933-720p-azhw` | 720p | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 5 | `seedance-2.0-vip-720p-azhw` | 720p | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 6 | `seedance-2.0-933-1080p-azhw` | 1080p | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 7 | `seedance-2.0-vip-1080p-azhw` | 1080p | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 8 | `seedance-2.0-933-4k-azhw` | 4K | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 9 | `seedance-2.0-vip-4k-azhw` | 4K | 4–15 | 0–9 | 3 | 0 | 按秒 |
| 10 | `seedance-933-pro-pi` | 720p | 固定 15 | 0–9 | 3 | 3 | 按次 |

前九个初始要求显式 duration；没有精确默认值证据时不依赖 Provider 默认。SD2 要求至少一张图且禁止
音频/视频；Pro PI 才允许 `reference_video`，按次费用不把 duration 当乘数。

## 2. 当前 size 证据

| Provider 模型 | resolution / ratio | Provider size | 实测产物 | 证据 |
| --- | --- | --- | --- | --- |
| Mini 720p | 720p / 16:9 | 1280x720 | 1280×720 | 2026-08-10 r1 |
| Fast 720p | 720p / 16:9 | 1280x720 | 1280×720 | 2026-08-10 r1 |
| Standard 720p | 720p / 16:9 | 1280x720 | 1280×720 | 2026-08-10 r1 |

当前只有 Mini 720p、Fast 720p 和 Standard 720p 的 16:9 组合进入代码 size 表。Standard
1080p/4K、Value 720p/1080p/4K、SD2 和 Pro PI 均未闭合；未有精确证据的组合不从旧版本或相邻模型
复制。

## 3. 客户计费

每个客户模型使用自己审批的 NEWAPI 价格和表达式：

- 九个按秒模型：冻结 duration、分辨率/size class、输入类型和分组倍率；
- Pro PI：冻结每次请求价格和已批准倍率，固定 15 秒只用于请求合同；
- Provider 预期成本和实际账单只供管理员对账，不覆盖客户结算；
- 预扣与终态使用同一冻结 probe；
- 所有乘数先校验上界，quota 使用 checked 饱和函数；
- 失败、退款和视频 unknown 按共享异步事实处理。

飞彩 `/v1/tasks` 在目标生产 HTTPS Base URL 实测 404，不能作为任务级对账源。subscription/usage 只
能作为账户趋势，不能按相近时间或模型猜测单任务成本。历史账户 usage 增量与研究报价存在不一致，
因此生产价格必须单独审批。

## 4. 不变量

1. 十个 Provider 模型分别配置客户模型、价格和证据，不合并档位。
2. 只维护当前 Provider 模型和当前 `url_media_arrays_v1` 协议，不恢复飞彩版本族。
3. 固定分辨率不能在请求期切到另一价格档。
4. 未验证 size、默认值、媒体或账单不由相邻模型推导。
5. 客户账单读取冻结 NEWAPI 计费和结算日志，不按当前 Provider 报价回算。
