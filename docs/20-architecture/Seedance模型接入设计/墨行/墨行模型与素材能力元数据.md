---
status: current
owner: Dev Team
last-reviewed: 2026-09-11
---

# 墨行模型与素材能力元数据

## 1. 公开投影

| 素材协议 | `assets.supported` | 组 | 真人 | `reuse_scope` |
| --- | --- | --- | --- | --- |
| `tokensave_assets_v1` | true | optional（Channel 默认组回退） | false | 按 Channel 素材租户身份派生 |
| `moxing_volc_assets_v1` | true | optional（Channel 默认组回退） | image | 按 Channel 素材租户身份派生 |
| `none` | false | unsupported | false | 空 |

共同字段：`management_mode=caller_managed_stateless`、`requires_model=true`、`reference_format=asset://{opaque_upstream_asset_id}`；列表操作 `supported=false`。普通素材组策略由 `GeneralAssetGroupPolicy` 统一发布；真人认证仅返回 Provider 会话/链接，不建立平台授权域。

## 2. 视频模型元数据

四个精确 Provider 模型统一登记于 `moxing_modelark_media_v1`，规则来源为
`relaykit/dto/moxing_video_models.go` 唯一注册表，运行时校验与公开投影共同读取：

| Provider 模型 | 时长 | 数量/参数限制 | 纯音频参考 |
| --- | --- | --- | --- |
| `doubao-seedance-2-0-260128-0818` | 4–15 或 -1（预算 15 秒） | 无本地数量枚举；已发布标准字段继续接受，实际效果待验收 | 接受（新页没有禁止） |
| `doubao-seedance-2-0-fast-260128` | 4–15 或 -1（预算 15 秒） | 无未证实数量上限；接受已发布标准字段 | 接受 |
| `doubao-seedance-2-0-mini-260615` | 4–15 或 -1（预算 15 秒） | 同 Fast | 接受 |
| `doubao-seedance-2-5-260628` | 4–30 或 -1（预算 30 秒） | 图≤30、视频≤10、音频≤10、总数≤50；支持 `seed` 与 `mp4/mov` | 接受 |

分辨率继续不设本地枚举（0818 页面列出 480p/720p/1080p/4k，Fast/Mini 只给建议），缺省发布并履约为 720p；未列出的值交由 Provider 判定。

0818 缺省输出声音为 true，显式 false 保持不变；本地接受纯音频不等于真实 Provider 已验收。
页面对 `output_format` 的明确不支持声明、未说明的 `duration=-1` 与本站已发布字段间的差异，
见[对接设计](墨行模型与素材库对接设计.md)的验收边界。

## 3. 元数据生成与隐藏

能力由 `video_upstream_protocol + Provider 模型` 的代码表生成，不由客户模型后缀或价格推断。公开响应不返回 Provider 名称、模型 ID、Channel、账号、Region/Project、Task 私有 ID 或原始 URL；`reuse_scope` 只表示同一非空作用域可尝试复用，不是存在性或权限证明。
