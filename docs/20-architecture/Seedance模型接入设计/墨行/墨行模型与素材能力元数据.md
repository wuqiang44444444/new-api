---
status: current
owner: Dev Team
last-reviewed: 2026-08-25
---

# 墨行模型与素材能力元数据

## 1. 公开投影

| 素材协议 | `assets.supported` | 组 | 真人 | `reuse_scope` |
| --- | --- | --- | --- | --- |
| `tokensave_assets_v1` | true | required | false | 按 Channel 素材租户身份派生 |
| `moxing_joycreator_assets_v1` | true | required | false | 按 Channel 素材租户身份派生 |
| `moxing_volc_assets_v1` | true | required | image | 按 Channel 素材租户身份派生 |
| `none` | false | unsupported | false | 空 |

共同字段：`management_mode=caller_managed_stateless`、`requires_model=true`、`reference_format=asset://{opaque_upstream_asset_id}`；列表操作 `supported=false`。TokenSave `/v1/asset`、JoyCreator 和 Moxing Volc 创建普通素材时必须携带素材组 ID，缺失时在 Provider 请求前拒绝。真人认证仅返回 Provider 会话/链接，不建立平台授权域。

## 2. 视频模型元数据

| Provider 模型 | 协议 | 分辨率 | 时长 | 素材上限/限制 |
| --- | --- | --- | --- | --- |
| `doubao-seedance-2-0-260128`（TokenSave/墨行） | 各自媒体任务协议 | 480/720p（TokenSave 资料另列 1080p，当前平台拒绝） | 4–15 或 -1 | 以协议文档为准；视频输入按线路不同 |
| `doubao-seedance-2-0-fast-260128` | `moxing_modelark_media_v1` | 480/720p | 4–15 | 图/视频/音频参考，≤9/3/3（代码约束） |
| `doubao-seedance-2-0-mini-260615` | `moxing_modelark_media_v1` | 480/720p | 4–15 | 同 Fast |
| `doubao-seedance-2-5-260628` | `moxing_modelark_media_v1` | 480/720p | 4–30 或 -1 | 图≤30、视频≤10、音频≤10；支持 seed |

## 3. 元数据生成与隐藏

能力由 `video_upstream_protocol + Provider 模型` 的代码表生成，不由客户模型后缀或价格推断。公开响应不返回 Provider 名称、模型 ID、Channel、账号、Region/Project、Task 私有 ID 或原始 URL；`reuse_scope` 只表示同一非空作用域可尝试复用，不是存在性或权限证明。
