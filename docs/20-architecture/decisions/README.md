---
status: current
owner: Dev Team
last-reviewed: 2026-08-17
---

# 架构决策索引

本目录保留已经接受以及后来被取代的架构决策。阅读当前事实时先看 `docs/20-architecture/` 的专题
架构，再按需进入对应 ADR；ADR 负责解释长期取舍、后果和替代关系，不作为当前实现清单。

ADR 编号是稳定引用，不是连续目录序号。编号按创建顺序只增不改，历史缺号不回填，现有编号不
重新排序；下一条新决策使用当前最大编号加一。

## 当前有效决策

| ADR | 唯一负责的决策边界 |
| --- | --- |
| [0008](0008-共享异步任务计费状态机与原子补偿.md) | 共享异步任务的资金目标、幂等结算与补偿 |
| [0017](0017-调用方自管无状态素材代理.md) | Seedance 调用方自管 opaque 素材 ID、无列表单资源代理和视频直通 |

## 已被取代的决策

| 原 ADR | 被取代者 | 说明 |
| --- | --- | --- |
| [0001](0001-视频上游协议适配与任务执行快照.md) | ADR-0016 | 删除 `DoubaoVideo` profile 扩展；第三方 Seedance 协议迁入专用渠道，原生渠道恢复上游语义 |
| [0005](0005-官方Action素材凭据隔离.md) | ADR-0016 | 保留官方视频/素材凭据分离，但改为协议驱动字段和 AssetGroup 认证代理 |
| [0009](0009-请求级媒体与平台托管素材双路径.md) | ADR-0016 | 保留 URL/Data URL 与平台素材双路径，删除通用 binding/迁移/独立授权域 |
| [0011](0011-异步创建未知与轮询合同违例对账.md) | ADR-0016 | 保留 durable attempt 与不可采信轮询响应隔离；删除 unknown 到期自动释放和专用核查体系 |
| [0015](0015-Link服务合同发布与实现身份绑定.md) | ADR-0016 | 删除 publication、SKU、implementation、execution binding 与候选等价体系 |
| [0016](0016-Seedance专用渠道与确定性素材代理.md) | ADR-0017 | 保留 Seedance 专用渠道和确定性视频执行；平台素材映射改为调用方自管无状态代理 |

## 不建立本地 ADR 的上游事实

NEWAPI 原生路由、DTO、Relay、Adapter、模型发现和客户端协议语义以上游代码为唯一权威，包括原生 `/v1/videos` 与 `/v1/video/generations`。同步上游能力不创建“恢复”“共存”或“兼容”ADR，也不得据此新增原生专属 Router、DTO、`client_protocol`、fallback、兼容中间件或历史协议推断。

Link 隔离只在 Link 专用 Router、ChannelType、代码 adapter、Task 和素材代理中完成。Link 文档不能
授权修改、复制、替换或收紧 NEWAPI 原生合同，也不能据模型名接管原生入口。

## 维护规则

- ADR 只记录长期、跨模块、难以逆转且存在真实备选方案的决策；实现步骤、兼容补丁、排障过程和当前代码清单不新建 ADR。
- ADR 编号和文件身份创建后不可更改。新决策取代旧决策时，旧 ADR 保留原编号并更新
  `status: superseded`、`superseded-by`；索引同时列出新旧关系。
- 当前有效 ADR 不把 superseded ADR 作为规范性前置条件；专题架构直接描述当前事实，仅在解释
  取舍时链接决策链。
- ADR 中的实现范围必须显式说明是否仅适用于 Link；未明确接入的 NEWAPI 原生路径默认不受本地 ADR 约束。
- 当前事实、路由和实现清单写入专题架构；ADR 只保留决定、原因、后果和未采用方案。
- 新 ADR 使用现有最大编号加一，不回填缺号，不因文件删除、标题优化或目录整理重新编号。
